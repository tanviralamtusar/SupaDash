# Database Configuration
POSTGRES_PASSWORD=postgres

# Security Secrets
# IMPORTANT: Generate strong secrets for production using: openssl rand -base64 48
# These must be at least 32 characters long
JWT_SECRET=change-me-in-production-min-32-chars
ENCRYPTION_SECRET=change-me-in-production-min-32-chars

# DNS Webhook Key
DOMAIN_DNS_HOOK_KEY=change-me-in-production

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. For local development, you can keep the defaults
# 3. For production, generate secure secrets:
#    openssl rand -base64 48
# 4. Never commit .env to version control
