# Local development — accessible at http://localhost
:80 {
    # API routes
    handle /v1/* {
        reverse_proxy api:8080
    }

    # Studio (default)
    handle {
        reverse_proxy studio:3000
    }
}
